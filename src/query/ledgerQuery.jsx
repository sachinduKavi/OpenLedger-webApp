import {header, mainDomain, temp_domain} from '../middleware/API'

const cDomain = temp_domain

const createLedgerRecord = async (record) => {
    // return await cDomain.
} 


export {
    createLedgerRecord
}